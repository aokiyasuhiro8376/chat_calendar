<%= form_with(model: room, local: true) do |form| %>
  <% if event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(room.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :タイトル %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :説明 %>
    <%= form.text_area :description %>
  </div>

  <div class="field">
    <%= form.label :開始 %>
    <%= form.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= form.label :終了 %>
    <%= form.datetime_select :end_date %>
  </div>

  <div class="actions">
    <%= form.submit :イベント追加%>
  </div>
<% end %>
